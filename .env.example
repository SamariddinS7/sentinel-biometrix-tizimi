# .env.example
# Secret key used for signing and verifying JWT tokens
JWT_SECRET=your_jwt_secret_key_here

# Google Gemini API Key for server-side AI processing (Keep this secret!)
GEMINI_API_KEY=

# VMS Credentials AES Encryption Key (Keep this secret!)
VMS_ENCRYPTION_KEY=
