# ─────────────────────────────────────────────────────────────────────────────
# DO NOT commit real secrets to source control.
# This file is a template only. Use Sealed Secrets, External Secrets Operator,
# or Vault Agent Injector to populate secrets in production clusters.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-vms-secrets
  namespace: sentinel-vms
  annotations:
    # Replace with Sealed Secrets controller annotation in production
    description: "Template — populate via CI/CD pipeline or secrets manager"
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_STRONG_RANDOM_256_BIT_SECRET"
  SESSION_SECRET: "REPLACE_WITH_STRONG_RANDOM_SECRET"
  VMS_ENCRYPTION_KEY: "REPLACE_WITH_32_BYTE_AES_KEY_HEX"
  POSTGRES_URL: "postgres://sentinel:REPLACE_PASSWORD@postgres-primary:5432/sentinel_vms?sslmode=require"
  POSTGRES_REPLICA_URL: "postgres://sentinel:REPLACE_PASSWORD@postgres-replica:5432/sentinel_vms?sslmode=require"
  REDIS_URL: "redis://:REPLACE_PASSWORD@redis-sentinel:26379"
  NATS_URL: "nats://nats:4222"
  STORAGE_ENDPOINT: "http://minio:9000"
  STORAGE_ACCESS_KEY: "REPLACE_MINIO_ACCESS_KEY"
  STORAGE_SECRET_KEY: "REPLACE_MINIO_SECRET_KEY"
  GEMINI_API_KEY: ""
